import { View } from '@/components/Layout/View'
import { Button } from '@radix-ui/themes'

export const Settings = () => {
  return (
    <View title="Settings" actions={<Button>Save</Button>}>
      Settings
    </View>
  )
}

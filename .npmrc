# Temporary: @typescript-eslint/typescript-estree v8 declares peerDependency
# typescript <6.0.0, but works fine at runtime with TS 6.0.
# Remove this once @typescript-eslint releases a version supporting TS 6.0.
legacy-peer-deps=true

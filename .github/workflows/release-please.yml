name: Release Please

on:
  push:
    branches:
      - main
      - ci/release-please-remove-pat

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
